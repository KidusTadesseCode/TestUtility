import styled from "styled-components";
export const PrompsContainer = styled.div``;
export const Header = styled.div``;

// Use styled-components and keep the style consistent
// Make me a component
